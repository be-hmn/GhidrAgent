[project]
name = "ghidramcp"
version = "0.1.0"
requires-python = ">=3.11,<3.12"

dependencies = [
    "pyhidra",
    "jpype1",
    "python-dotenv",
    "mcp>=1.0.0",
]

[project.scripts]
mcp-smoke = "scripts.mcp_smoke:main"
ghidra-agent = "main:main"
ghidra-agent-mcp = "main:_main_mcp"

[project]
name = "ghidragent"
version = "0.1.0"
requires-python = ">=3.11,<3.12"

dependencies = [
    "pyhidra",
    "jpype1",
    "python-dotenv",
]

[project.scripts]
mcp-smoke = "scripts.mcp_smoke:main"
